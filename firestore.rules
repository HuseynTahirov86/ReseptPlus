
rules_version = '2';

// Bu şərh faylın yenidən yüklənməsini tətikləmək üçün əlavə edilib.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
