import { BlogClientPage } from "./client-page";

export default function AdminBlogPage() {
  // Data fetching is now handled client-side with useCollection for real-time updates.
  return <BlogClientPage />;
}
