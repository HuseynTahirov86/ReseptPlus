import { PharmaciesClientPage } from "./client-page";

export default function AdminPharmaciesPage() {
  // Data fetching is now handled client-side for real-time updates.
  return <PharmaciesClientPage />;
}
